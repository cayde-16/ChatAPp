commands:
  start:
    - name: Start Server
      command: cd server && npm start
    - name: Start Client
      command: cd client && npm start
    - name: Start Both
      command: railway run start:server && railway run start:client
